// Aqui se define la interfaz IEstudiante, que en este caso representa la estructura de datos de un estudiante
export interface IEstudiante {
    id?: number;
    name: string;
    email: string;
    registrationDate: string;
  }
  